export default function GET(req: Request) {
    try {
        const hdrs = req.headers

        console.log("Headers: ", hdrs.entries())

        const DEFAULT_CURRENCY_RATE = "PHP/USD"


    } catch (error) {

    }
}